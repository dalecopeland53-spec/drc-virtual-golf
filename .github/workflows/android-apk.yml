name: Android APK Legacy Workflow Disabled

on:
  workflow_dispatch:

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Use the Expo Android workflow
        run: echo "This legacy Capacitor workflow is disabled for push builds. Use .github/workflows/build.yml for the Expo React Native APK."
